# This file was generated using the `serve build` command on Ray v2.4.0.

import_path: translator:serve_dag

runtime_env: {}

host: 0.0.0.0

port: 8000

deployments:

- name: Translator
  num_replicas: 2
  ray_actor_options:
    num_cpus: 0.2
    num_gpus: 0.0

- name: DAGDriver
  route_prefix: /
